[build-system]
requires = ["setuptools>=83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "password-policy-state-space"
version = "0.1.0"
description = "Exact counting and uniform sampling for constrained password policies"
authors = [{ name = "Omar Ibrahim" }]
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["Flask==3.1.3", "waitress==3.0.2"]

[project.scripts]
password-policy-lab = "password_policy_lab.cli:main"

[project.optional-dependencies]
dev = [
    "build==1.5.0",
    "mypy==2.3.0",
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "ruff==0.16.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
password_policy_lab = ["py.typed", "templates/*.html"]

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["B", "E", "F", "I", "RUF", "SIM", "UP"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.11"
strict = true
warn_unreachable = true

[tool.pytest.ini_options]
addopts = ["--strict-config", "--strict-markers"]
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["password_policy_lab"]

[tool.coverage.report]
fail_under = 100
show_missing = true
skip_covered = true
